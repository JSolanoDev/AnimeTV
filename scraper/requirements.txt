# AnimeTV Metadata Scraper — Python dependencies
#
# Install:
#   pip install -r requirements.txt
#   scrapling install        ← downloads Camoufox browser binaries (~200 MB, one-time)
#
# Python >= 3.11 recommended (uses str | None union syntax)

scrapling[all]>=0.3.0
