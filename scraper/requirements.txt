# AnimeTV Metadata Scraper — Python dependencies
#
# Install:
#   pip install -r requirements.txt
#
# No browser installation required.
# Python >= 3.10 required (uses str | None union syntax in 3.10+, or just Python 3.11+).

requests>=2.32
urllib3>=2.2
